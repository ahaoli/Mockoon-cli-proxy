{
  "name": "mockoon-cli-proxy",
  "version": "1.0.0",
  "description": "Proxy Mockoon CLI responses as streaming SSE-like chunks",
  "main": "src/index.js",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/index.js",
    "start:daemon": "bash start.sh",
    "stop:daemon": "bash scripts/stop.sh",
    "status:daemon": "if [ -f logs/proxy.pid ] && kill -0 $(cat logs/proxy.pid) 2>/dev/null; then echo \"running pid $(cat logs/proxy.pid)\"; else echo \"not running\"; fi",
    "test": "node --check src/index.js"
  }
}
